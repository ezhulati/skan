---
// Cookie Consent Component for GDPR compliance
---

<div id="cookie-banner" class="fixed bottom-0 left-0 right-0 bg-gray-900 text-white py-3 px-4 z-50 shadow-lg" style="display: none;">
  <div class="max-w-7xl mx-auto">
    <div class="flex flex-col sm:flex-row items-start sm:items-center gap-3">
      <div class="flex-1 min-w-0">
        <p class="text-sm text-gray-200 leading-snug" data-sq="Ne përdorim cookies për të përmirësuar përvojën tuaj. " data-en="We use cookies to enhance your experience. ">
          Ne përdorim cookies për të përmirësuar përvojën tuaj. 
          <a href="/privacy" class="text-blue-400 hover:text-blue-300 underline" data-sq="Mësoni më shumë" data-en="Learn more">Mësoni më shumë</a>
        </p>
      </div>
      
      <div class="flex gap-2 w-full sm:w-auto">
        <button 
          id="cookie-settings" 
          class="flex-1 sm:flex-none px-3 py-2 border border-gray-500 text-gray-300 hover:bg-gray-800 rounded text-xs font-medium transition-colors"
          data-sq="Cilësimet" 
          data-en="Settings"
        >
          Cilësimet
        </button>
        <button 
          id="cookie-accept-selected" 
          class="flex-1 sm:flex-none px-3 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded text-xs font-medium transition-colors"
          data-sq="Thelbësore" 
          data-en="Essential"
        >
          Thelbësore
        </button>
        <button 
          id="cookie-accept-all" 
          class="flex-1 sm:flex-none px-3 py-2 bg-green-600 hover:bg-green-700 text-white rounded text-xs font-medium transition-colors"
          data-sq="Prano të Gjitha" 
          data-en="Accept All"
        >
          Prano të Gjitha
        </button>
      </div>
    </div>
  </div>
</div>

<!-- Cookie Settings Modal -->
<div id="cookie-modal" class="fixed inset-0 bg-black bg-opacity-50 z-50 hidden">
  <div class="flex items-center justify-center min-h-screen p-4">
    <div class="bg-white rounded-lg max-w-2xl w-full max-h-[90vh] overflow-y-auto">
      <div class="p-6">
        <div class="flex justify-between items-center mb-4">
          <h3 class="text-xl font-bold text-gray-900" data-sq="Cilësimet e Cookies" data-en="Cookie Settings">Cilësimet e Cookies</h3>
          <button id="cookie-modal-close" class="text-gray-400 hover:text-gray-600">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </button>
        </div>
        
        <p class="text-gray-600 mb-6" data-sq="Zgjidhni llojet e cookies që dëshironi të pranoni. Ju mund t'i ndryshoni këto cilësime në çdo kohë." data-en="Choose which types of cookies you want to accept. You can change these settings at any time.">
          Zgjidhni llojet e cookies që dëshironi të pranoni. Ju mund t'i ndryshoni këto cilësime në çdo kohë.
        </p>
        
        <div class="space-y-4">
          <!-- Essential Cookies -->
          <div class="border border-gray-200 rounded-lg p-4">
            <div class="flex items-center justify-between mb-2">
              <label class="flex items-center">
                <input type="checkbox" checked disabled class="mr-3 h-4 w-4 text-blue-600 opacity-50">
                <span class="font-medium text-gray-900" data-sq="Cookies Thelbësore" data-en="Essential Cookies">Cookies Thelbësore</span>
              </label>
              <span class="text-xs text-gray-500 px-2 py-1 bg-gray-100 rounded" data-sq="Të detyrueshme" data-en="Required">Të detyrueshme</span>
            </div>
            <p class="text-sm text-gray-600" data-sq="Këto cookies janë të nevojshme për funksionimin bazik të faqes së internetit dhe nuk mund të çaktivizohen." data-en="These cookies are necessary for the basic functioning of the website and cannot be disabled.">
              Këto cookies janë të nevojshme për funksionimin bazik të faqes së internetit dhe nuk mund të çaktivizohen.
            </p>
          </div>
          
          <!-- Analytics Cookies -->
          <div class="border border-gray-200 rounded-lg p-4">
            <div class="flex items-center justify-between mb-2">
              <label class="flex items-center">
                <input type="checkbox" id="analytics-cookies" class="mr-3 h-4 w-4 text-blue-600">
                <span class="font-medium text-gray-900" data-sq="Cookies Analize" data-en="Analytics Cookies">Cookies Analize</span>
              </label>
            </div>
            <p class="text-sm text-gray-600" data-sq="Na ndihmojnë të kuptojmë si përdoruesit ndërveprojnë me faqen tonë duke mbledhur dhe raportuar informacion në mënyrë anonime." data-en="Help us understand how users interact with our website by collecting and reporting information anonymously.">
              Na ndihmojnë të kuptojmë si përdoruesit ndërveprojnë me faqen tonë duke mbledhur dhe raportuar informacion në mënyrë anonime.
            </p>
          </div>
          
          <!-- Marketing Cookies -->
          <div class="border border-gray-200 rounded-lg p-4">
            <div class="flex items-center justify-between mb-2">
              <label class="flex items-center">
                <input type="checkbox" id="marketing-cookies" class="mr-3 h-4 w-4 text-blue-600">
                <span class="font-medium text-gray-900" data-sq="Cookies Marketingu" data-en="Marketing Cookies">Cookies Marketingu</span>
              </label>
            </div>
            <p class="text-sm text-gray-600" data-sq="Përdoren për të gjurmuar vizitorët nëpër faqe të internetit me qëllimin e shfaqjes së reklamave që janë relevante dhe angazhuese." data-en="Used to track visitors across websites with the intention of displaying ads that are relevant and engaging.">
              Përdoren për të gjurmuar vizitorët nëpër faqe të internetit me qëllimin e shfaqjes së reklamave që janë relevante dhe angazhuese.
            </p>
          </div>
          
          <!-- Functional Cookies -->
          <div class="border border-gray-200 rounded-lg p-4">
            <div class="flex items-center justify-between mb-2">
              <label class="flex items-center">
                <input type="checkbox" id="functional-cookies" class="mr-3 h-4 w-4 text-blue-600">
                <span class="font-medium text-gray-900" data-sq="Cookies Funksionesh" data-en="Functional Cookies">Cookies Funksionesh</span>
              </label>
            </div>
            <p class="text-sm text-gray-600" data-sq="Mundësojnë funksionalitete të përmirësuara dhe personalizim, si videot dhe chat-i live." data-en="Enable enhanced functionality and personalization, such as videos and live chat.">
              Mundësojnë funksionalitete të përmirësuara dhe personalizim, si videot dhe chat-i live.
            </p>
          </div>
        </div>
        
        <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-200">
          <button 
            id="cookie-save-settings" 
            class="px-6 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg font-medium transition-colors"
            data-sq="Ruaj Cilësimet" 
            data-en="Save Settings"
          >
            Ruaj Cilësimet
          </button>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  // Cookie consent functionality
  (function() {
    const COOKIE_NAME = 'skan_cookie_consent';
    const CONSENT_EXPIRY = 365; // days
    
    // Check if consent already given
    function hasConsent() {
      return document.cookie.indexOf(COOKIE_NAME + '=') !== -1;
    }
    
    // Set cookie with consent preferences
    function setConsent(preferences) {
      const date = new Date();
      date.setTime(date.getTime() + (CONSENT_EXPIRY * 24 * 60 * 60 * 1000));
      const expires = "expires=" + date.toUTCString();
      document.cookie = COOKIE_NAME + "=" + JSON.stringify(preferences) + ";" + expires + ";path=/;SameSite=Lax";
    }
    
    // Get current consent preferences
    function getConsent() {
      const name = COOKIE_NAME + "=";
      const decodedCookie = decodeURIComponent(document.cookie);
      const ca = decodedCookie.split(';');
      for(let i = 0; i < ca.length; i++) {
        let c = ca[i];
        while (c.charAt(0) == ' ') {
          c = c.substring(1);
        }
        if (c.indexOf(name) == 0) {
          try {
            return JSON.parse(c.substring(name.length, c.length));
          } catch (e) {
            return null;
          }
        }
      }
      return null;
    }
    
    // Initialize Google Analytics (only if consent given)
    function initializeAnalytics() {
      const consent = getConsent();
      if (consent && consent.analytics) {
        // Initialize Google Analytics here when ready
        console.log('Analytics cookies accepted - GA would be initialized here');
      }
    }
    
    // Initialize marketing tools (only if consent given)
    function initializeMarketing() {
      const consent = getConsent();
      if (consent && consent.marketing) {
        // Initialize marketing pixels/tools here when ready
        console.log('Marketing cookies accepted - tools would be initialized here');
      }
    }
    
    // Show/hide banner
    function showBanner() {
      document.getElementById('cookie-banner').style.display = 'block';
    }
    
    function hideBanner() {
      document.getElementById('cookie-banner').style.display = 'none';
    }
    
    // Show/hide modal
    function showModal() {
      document.getElementById('cookie-modal').classList.remove('hidden');
      
      // Load current preferences
      const consent = getConsent() || { essential: true, analytics: false, marketing: false, functional: false };
      document.getElementById('analytics-cookies').checked = consent.analytics;
      document.getElementById('marketing-cookies').checked = consent.marketing;
      document.getElementById('functional-cookies').checked = consent.functional;
    }
    
    function hideModal() {
      document.getElementById('cookie-modal').classList.add('hidden');
    }
    
    // Event listeners
    document.addEventListener('DOMContentLoaded', function() {
      // Show banner if no consent
      if (!hasConsent()) {
        showBanner();
      } else {
        // Initialize tools based on existing consent
        initializeAnalytics();
        initializeMarketing();
      }
      
      // Accept all cookies
      document.getElementById('cookie-accept-all').addEventListener('click', function() {
        const preferences = {
          essential: true,
          analytics: true,
          marketing: true,
          functional: true,
          timestamp: new Date().toISOString()
        };
        setConsent(preferences);
        hideBanner();
        initializeAnalytics();
        initializeMarketing();
      });
      
      // Accept selected cookies
      document.getElementById('cookie-accept-selected').addEventListener('click', function() {
        const preferences = {
          essential: true,
          analytics: false,
          marketing: false,
          functional: false,
          timestamp: new Date().toISOString()
        };
        setConsent(preferences);
        hideBanner();
      });
      
      // Show settings modal
      document.getElementById('cookie-settings').addEventListener('click', function() {
        showModal();
      });
      
      // Close modal
      document.getElementById('cookie-modal-close').addEventListener('click', hideModal);
      
      // Save custom settings
      document.getElementById('cookie-save-settings').addEventListener('click', function() {
        const preferences = {
          essential: true, // Always true
          analytics: document.getElementById('analytics-cookies').checked,
          marketing: document.getElementById('marketing-cookies').checked,
          functional: document.getElementById('functional-cookies').checked,
          timestamp: new Date().toISOString()
        };
        setConsent(preferences);
        hideModal();
        hideBanner();
        
        // Initialize tools based on preferences
        if (preferences.analytics) initializeAnalytics();
        if (preferences.marketing) initializeMarketing();
      });
      
      // Close modal when clicking outside
      document.getElementById('cookie-modal').addEventListener('click', function(e) {
        if (e.target === this) {
          hideModal();
        }
      });
    });
  })();
</script>

<style>
  /* Ensure banner appears above other content */
  #cookie-banner {
    z-index: 9999;
  }
  
  #cookie-modal {
    z-index: 10000;
  }
  
  /* Animation for banner */
  #cookie-banner {
    animation: slideUp 0.3s ease-out;
  }
  
  @keyframes slideUp {
    from {
      transform: translateY(100%);
    }
    to {
      transform: translateY(0);
    }
  }
</style>